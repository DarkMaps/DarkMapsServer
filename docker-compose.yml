version: '3'

services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: sadkjghlwh498jksdghlk4wtywaeuht98434
      MYSQL_DATABASE: signal_server
    healthcheck:
          test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
          timeout: 20s
          retries: 10
  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - "db"
    command: bash -c "sleep 30 && python manage.py && ./start.sh"